apply from: 'javafx.plugin'

repositories {
	mavenCentral()
	mavenLocal()
	jcenter()
}

dependencies {
    compile project(':FERemoteLib')
}

jar {
	from configurations.compile.collect { it }
	exclude('jfxrt.jar')
}