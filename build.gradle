apply from: 'javafx.plugin'

rootProject.name = 'FERemoteClient'

repositories {
	mavenCentral()
	mavenLocal()
	jcenter()
}

dependencies {
    compile project(':FERemoteLib')
}

jar {
	from configurations.compile.collect { it }
	exclude('jfxrt.jar')
}